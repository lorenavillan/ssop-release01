<%= form_for(@tipo_certificado) do |f| %>
  <% if @tipo_certificado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_certificado.errors.count, "error") %> prohibited this tipo_certificado from being saved:</h2>

      <ul>
      <% @tipo_certificado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_certificado_descripcion %><br />
    <%= f.text_field :tipo_certificado_descripcion %>
  </div>
  <div class="field">
    <%= f.label :deleted_at %><br />
    <%= f.datetime_select :deleted_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

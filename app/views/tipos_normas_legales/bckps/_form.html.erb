<%= form_for(@tipo_norma_legal) do |f| %>
  <% if @tipo_norma_legal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_norma_legal.errors.count, "error") %> prohibited this tipo_norma_legal from being saved:</h2>

      <ul>
      <% @tipo_norma_legal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_norma_legal_descripcion %><br />
    <%= f.text_field :tipo_norma_legal_descripcion %>
  </div>
  <div class="field">
    <%= f.label :deleted_at %><br />
    <%= f.datetime_select :deleted_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
